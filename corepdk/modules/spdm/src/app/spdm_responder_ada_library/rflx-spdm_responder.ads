--
--  Generated by RecordFlux 0.6.1.dev1+g8b82b38a0 on 2022-12-02
--
--  Copyright (C) 2018-2022 AdaCore
--
--  SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
--

pragma Style_Checks ("N3aAbcdefhiIklnOprStux");
pragma Warnings (Off, "redundant conversion");
with RFLX.RFLX_Types;

package RFLX.SPDM_Responder with
  SPARK_Mode
is

   type DHE_Algo is (DA_Unsupported, DA_FFDHE2048, DA_FFDHE3072, DA_FFDHE4096, DA_SecP256r1, DA_SecP384r1, DA_SecP521r1) with
     Size =>
       8;
   for DHE_Algo use (DA_Unsupported => 0, DA_FFDHE2048 => 1, DA_FFDHE3072 => 2, DA_FFDHE4096 => 4, DA_SecP256r1 => 8, DA_SecP384r1 => 16, DA_SecP521r1 => 32);

   use type RFLX.RFLX_Types.Base_Integer;

   function Valid_DHE_Algo (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 0 | 1 | 2 | 4 | 8 | 16 | 32);

   function To_Base_Integer (Enum : RFLX.SPDM_Responder.DHE_Algo) return RFLX.RFLX_Types.Base_Integer is
     ((case Enum is
          when DA_Unsupported =>
             0,
          when DA_FFDHE2048 =>
             1,
          when DA_FFDHE3072 =>
             2,
          when DA_FFDHE4096 =>
             4,
          when DA_SecP256r1 =>
             8,
          when DA_SecP384r1 =>
             16,
          when DA_SecP521r1 =>
             32));

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Responder.DHE_Algo is
     ((case Val is
          when 0 =>
             DA_Unsupported,
          when 1 =>
             DA_FFDHE2048,
          when 2 =>
             DA_FFDHE3072,
          when 4 =>
             DA_FFDHE4096,
          when 8 =>
             DA_SecP256r1,
          when 16 =>
             DA_SecP384r1,
          when 32 =>
             DA_SecP521r1,
          when others =>
             RFLX.SPDM_Responder.DHE_Algo'Last))
    with
     Pre =>
       Valid_DHE_Algo (Val);

   pragma Warnings (On, "unreachable branch");

   type AEAD_Algo is (AA_Unsupported, AA_AES_128_GCM, AA_AES_256_GCM, AA_ChaCha20_Poly1305) with
     Size =>
       8;
   for AEAD_Algo use (AA_Unsupported => 0, AA_AES_128_GCM => 1, AA_AES_256_GCM => 2, AA_ChaCha20_Poly1305 => 4);

   function Valid_AEAD_Algo (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 0 | 1 | 2 | 4);

   function To_Base_Integer (Enum : RFLX.SPDM_Responder.AEAD_Algo) return RFLX.RFLX_Types.Base_Integer is
     ((case Enum is
          when AA_Unsupported =>
             0,
          when AA_AES_128_GCM =>
             1,
          when AA_AES_256_GCM =>
             2,
          when AA_ChaCha20_Poly1305 =>
             4));

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Responder.AEAD_Algo is
     ((case Val is
          when 0 =>
             AA_Unsupported,
          when 1 =>
             AA_AES_128_GCM,
          when 2 =>
             AA_AES_256_GCM,
          when 4 =>
             AA_ChaCha20_Poly1305,
          when others =>
             RFLX.SPDM_Responder.AEAD_Algo'Last))
    with
     Pre =>
       Valid_AEAD_Algo (Val);

   pragma Warnings (On, "unreachable branch");

   type Digests_Length is range 0 .. 512 with
     Size =>
       16;

   function Valid_Digests_Length (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 512);

   function To_Base_Integer (Val : RFLX.SPDM_Responder.Digests_Length) return RFLX.RFLX_Types.Base_Integer is
     (RFLX.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Responder.Digests_Length is
     (RFLX.SPDM_Responder.Digests_Length (Val))
    with
     Pre =>
       Valid_Digests_Length (Val);

   type Transcript_ID is mod 2**32 with
     Size =>
       32;

   function Valid_Transcript_ID (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 4294967295);

   function To_Base_Integer (Val : RFLX.SPDM_Responder.Transcript_ID) return RFLX.RFLX_Types.Base_Integer is
     (RFLX.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Responder.Transcript_ID is
     (RFLX.SPDM_Responder.Transcript_ID (Val))
    with
     Pre =>
       Valid_Transcript_ID (Val);

   type Transcript_Kind is (Session_Transcript, Measurement_Transcript) with
     Size =>
       8;
   for Transcript_Kind use (Session_Transcript => 0, Measurement_Transcript => 1);

   function Valid_Transcript_Kind (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 0 | 1);

   function To_Base_Integer (Enum : RFLX.SPDM_Responder.Transcript_Kind) return RFLX.RFLX_Types.Base_Integer is
     ((case Enum is
          when Session_Transcript =>
             0,
          when Measurement_Transcript =>
             1));

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Responder.Transcript_Kind is
     ((case Val is
          when 0 =>
             Session_Transcript,
          when 1 =>
             Measurement_Transcript,
          when others =>
             RFLX.SPDM_Responder.Transcript_Kind'Last))
    with
     Pre =>
       Valid_Transcript_Kind (Val);

   pragma Warnings (On, "unreachable branch");

end RFLX.SPDM_Responder;
