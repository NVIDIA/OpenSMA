--
--  Generated by RecordFlux 0.6.1.dev1+g8b82b38a0 on 2022-12-02
--
--  Copyright (C) 2018-2022 AdaCore
--
--  SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
--

pragma Style_Checks ("N3aAbcdefhiIklnOprStux");
pragma Warnings (Off, "redundant conversion");
with RFLX.RFLX_Types;

package RFLX.SPDM_Emu with
  SPARK_Mode
is

   type Command is (Normal, Shutdown) with
     Size =>
       32;
   for Command use (Normal => 1, Shutdown => 65534);

   use type RFLX.RFLX_Types.Base_Integer;

   function Valid_Command (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 16#1# | 16#FFFE#);

   function To_Base_Integer (Enum : RFLX.SPDM_Emu.Command) return RFLX.RFLX_Types.Base_Integer is
     ((case Enum is
          when Normal =>
             16#1#,
          when Shutdown =>
             16#FFFE#));

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Emu.Command is
     ((case Val is
          when 16#1# =>
             Normal,
          when 16#FFFE# =>
             Shutdown,
          when others =>
             RFLX.SPDM_Emu.Command'Last))
    with
     Pre =>
       Valid_Command (Val);

   pragma Warnings (On, "unreachable branch");

   type Transport_Type is (None) with
     Size =>
       32;
   for Transport_Type use (None => 0);

   function Valid_Transport_Type (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val in 0);

   function To_Base_Integer (Enum : RFLX.SPDM_Emu.Transport_Type) return RFLX.RFLX_Types.Base_Integer is
     ((case Enum is
          when None =>
             0));

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Emu.Transport_Type is
     ((case Val is
          when 0 =>
             None,
          when others =>
             RFLX.SPDM_Emu.Transport_Type'Last))
    with
     Pre =>
       Valid_Transport_Type (Val);

   pragma Warnings (On, "unreachable branch");

   type Size is range 0 .. 2**24 - 1 with
     Size =>
       32;

   function Valid_Size (Val : RFLX.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 1677722);

   function To_Base_Integer (Val : RFLX.SPDM_Emu.Size) return RFLX.RFLX_Types.Base_Integer is
     (RFLX.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : RFLX.RFLX_Types.Base_Integer) return RFLX.SPDM_Emu.Size is
     (RFLX.SPDM_Emu.Size (Val))
    with
     Pre =>
       Valid_Size (Val);

end RFLX.SPDM_Emu;
